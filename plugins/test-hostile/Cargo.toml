[workspace]

[package]
name = "anistream-hostile-plugin"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A deliberately misbehaving plugin, used to prove the sandbox holds"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.60"

[profile.release]
opt-level = "s"
lto = true
strip = true
